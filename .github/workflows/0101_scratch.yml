name: Build and Push Docker Image 0101_scratch

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: 0101_scratch
  REGISTRY: ghcr.io
  USERNAME: nulshh
  TAG: 1.0.0
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.MY_USERNAME }}
          password: ${{ secrets.MY_TOKEN }}

      - name: Clear Docker cache
        run: docker builder prune --all

#    - name: Set environment variables
#      id: env_vars
#      run: echo "::set-output name=repo::${{ env.GITHUB_REPOSITORY }}, actor::${{ env.GITHUB_ACTOR }}, event::${{ env.GITHUB_EVENT_NAME }}"
      - name: Telegram sender
  # You may pin to the exact commit or the version.
  # uses: robotomize/notify-telegram@f2821d117115d48ccbbde7609d2029603ee55b7e
        uses: robotomize/notify-telegram@v0.2.4
        with:
    # Chat to send: chat id or @channel_name
          chat: -1001948676627
    # Telegram Bot token
          token: 6081641656:AAE2PUsNkNvpVvlwsxymfBfKSKF42VXD2hU
    # Job status
          status: Deployed by Actor ${{ github.actor }} workspace ${{ github.workspace }}
    # Custom message
          message: "Wow! We have updates: in repository ${{ github.repository }} with commit ${{ github.sha }} Event: ${{ github.event_name }} github action ${{ github.action }}"

