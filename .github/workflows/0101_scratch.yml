name: Build and Push Docker Image 0101_scratch

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: 0101_scratch
  REGISTRY: ghcr.io
  USERNAME: nulshh
  TAG: 1.0.0
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.MY_USERNAME }}
          password: ${{ secrets.MY_TOKEN }}

      - name: Clear Docker cache
        run: docker builder prune --all

      - name: Telegram Notifier
  # You may pin to the exact commit or the version.
  # uses: nikhilbadyal/telegram-forwarder@3b6d277f571083b6f0a74b1cafdbc964151c8501
        uses: nikhilbadyal/telegram-forwarder@0.0.1
        with:
    # Telegram Bot Token
          token: 6081641656:AAE2PUsNkNvpVvlwsxymfBfKSKF42VXD2hU
    # Message to be sent
          message:  optional, default is Action Finished
    # Receiver of the message
          recipient: -1001948676627
